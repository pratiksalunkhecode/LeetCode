class Solution {
    public static int largest(int[] arr) {
        // code here
        int n = arr.length;
        int res = 0;
        
        for(int i = 0; i < n; i++)
        {
            if(arr[i] > res)
            {
                res = arr[i];
            }
        }
        return res;
    }
}
